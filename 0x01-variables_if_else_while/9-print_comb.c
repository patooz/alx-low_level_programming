#include <stdio.h>
/**
 * main - Entry point
 * Description: combinations of single-digit numbers
 * Retutn: 0 (success)
 */
int main(void)
{
	char i;

	for (i = 48; i <= 48; i++)
	{
		putchar((char)i);
		if (i != 57)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
